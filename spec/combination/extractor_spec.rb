require 'spec_helper'

describe Combination::Extractor do
  it 'has a version number' do
    expect(Combination::Extractor::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
